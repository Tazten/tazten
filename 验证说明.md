# Supabase 验证说明

## 项目信息

- **项目名**: Tazten
- **表名**: transactions

## 验证步骤

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

确保 `.env.local` 文件包含以下变量：

```bash
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

### 3. 运行验证脚本

```bash
npm run verify:supabase
```

或者直接使用 tsx：

```bash
npx tsx scripts/verify-supabase.ts
```

## 验证内容

脚本会检查以下内容：

1. ✅ **环境变量** - 确认所有必需的变量都已设置
2. ✅ **客户端连接** - 测试使用 anon key 的连接
3. ✅ **服务端连接** - 测试使用 service role key 的连接
4. ✅ **表结构** - 验证 transactions 表是否存在
5. ✅ **字段完整性** - 检查所有必需字段是否存在
6. ✅ **插入测试** - 测试插入和删除操作

## 预期结果

如果一切正常，你会看到：

```
🔍 开始验证 Supabase 连接...

1️⃣ 检查环境变量:
   ✅ NEXT_PUBLIC_SUPABASE_URL: https://xxx.supabase.co
   ✅ NEXT_PUBLIC_SUPABASE_ANON_KEY: eyJ...
   ✅ SUPABASE_SERVICE_ROLE_KEY: eyJ...

2️⃣ 测试客户端连接:
   ✅ 客户端连接成功

3️⃣ 测试服务端连接:
   ✅ 服务端连接成功

4️⃣ 验证表结构:
   ✅ 表结构验证通过
   ✅ 插入测试成功，记录 ID: xxx
   ✅ 测试数据已清理

5️⃣ 检查表字段:
   ✅ 所有必需字段都存在 (共 15 个字段)

✅ Supabase 验证完成！所有检查通过。

📋 项目信息:
   项目名: Tazten
   表名: transactions
   URL: https://xxx.supabase.co
```

## 常见问题

### 1. 表不存在

如果看到 `relation "transactions" does not exist`：

1. 登录 Supabase Dashboard
2. 进入 SQL Editor
3. 执行 `supabase/schema.sql` 中的 SQL 语句

### 2. 字段缺失

如果某些字段不存在：

1. 检查 `supabase/schema.sql` 中的字段定义
2. 在 Supabase Dashboard → Table Editor 中查看表结构
3. 使用 ALTER TABLE 添加缺失的字段

### 3. 权限错误

如果看到权限相关错误：

1. 确认使用了正确的 API Key
2. 检查 RLS (Row Level Security) 策略
3. 对于服务端操作，使用 Service Role Key

## 下一步

验证通过后，可以：

1. 测试图片上传功能
2. 测试 API 路由 `/api/parse-and-save`
3. 查看 Supabase Dashboard 中的实际数据

