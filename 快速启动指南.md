# 快速启动指南

## 🚀 启动开发服务器

### 1. 切换到项目目录

```bash
cd /Users/tangziteng/github/tazten
```

### 2. 启动开发服务器

```bash
npm run dev
```

### 3. 访问应用

打开浏览器访问：http://localhost:3000

## ⚠️ 常见问题

### 问题：Missing script: "dev"

**原因**：在错误的目录运行命令

**解决**：
1. 确认你在项目目录中：`cd /Users/tangziteng/github/tazten`
2. 确认 `package.json` 文件存在：`ls package.json`
3. 然后再运行：`npm run dev`

### 问题：端口被占用

**解决**：
```bash
# 使用其他端口
PORT=3001 npm run dev
```

### 问题：依赖未安装

**解决**：
```bash
npm install
```

## 📋 完整启动流程

```bash
# 1. 进入项目目录
cd /Users/tangziteng/github/tazten

# 2. 安装依赖（如果还没安装）
npm install

# 3. 启动开发服务器
npm run dev

# 4. 打开浏览器访问
# http://localhost:3000
```

## 🔍 验证环境

启动前确认：
- [ ] 在正确的目录：`/Users/tangziteng/github/tazten`
- [ ] `package.json` 文件存在
- [ ] `.env.local` 文件已配置
- [ ] `node_modules` 目录存在（如果不存在，运行 `npm install`）

---

**现在切换到项目目录，然后运行 `npm run dev`！** 🚀

