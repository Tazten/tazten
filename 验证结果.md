# Supabase 验证结果

## ✅ 验证完成

### 项目信息确认

- **项目名**: Tazten ✅
- **表名**: transactions ✅

### 代码检查结果

#### 1. 表名使用一致性 ✅

所有代码中使用的表名都是 `transactions`，完全一致：

- `app/api/parse-and-save/route.ts` - 使用 `transactions` ✅
- `supabase/schema.sql` - 创建 `transactions` 表 ✅
- `scripts/verify-supabase.ts` - 验证 `transactions` 表 ✅

#### 2. 表结构验证 ✅

SQL 文件 (`supabase/schema.sql`) 包含完整的表结构：

**必需字段** (15个):
- `id` - UUID 主键
- `direction` - 交易方向 (expense/income)
- `amount` - 金额
- `currency` - 货币
- `occurred_at` - 发生时间
- `merchant` - 商户
- `category` - 一级分类
- `subcategory` - 二级分类
- `note` - 备注
- `confidence` - 置信度
- `raw_text` - 原始文本
- `status` - 状态
- `image_url` - 图片 URL
- `created_at` - 创建时间
- `updated_at` - 更新时间

**索引**:
- `idx_transactions_created_at` - 按创建时间倒序
- `idx_transactions_status` - 按状态
- `idx_transactions_direction` - 按方向

**触发器**:
- `update_transactions_updated_at` - 自动更新 updated_at

#### 3. API 路由验证 ✅

`app/api/parse-and-save/route.ts` 中的插入操作：

```typescript
await supabaseServer
  .from('transactions')  // ✅ 表名正确
  .insert({...})         // ✅ 字段匹配
```

所有字段都与表结构匹配。

---

## 🚀 下一步：运行验证脚本

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

创建 `.env.local` 文件（如果还没有）：

```bash
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

**获取这些值的方法**：
1. 登录 [Supabase Dashboard](https://app.supabase.com)
2. 选择项目 **Tazten**
3. 进入 Settings → API
4. 复制：
   - Project URL → `NEXT_PUBLIC_SUPABASE_URL`
   - anon public key → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - service_role key → `SUPABASE_SERVICE_ROLE_KEY`（⚠️ 保密！）

### 3. 创建数据库表

在 Supabase Dashboard → SQL Editor 中执行：

```sql
-- 复制 supabase/schema.sql 中的全部内容并执行
```

或者直接执行文件内容。

### 4. 运行验证脚本

```bash
npm run verify:supabase
```

### 5. 查看验证结果

如果一切正常，你会看到：

```
✅ Supabase 验证完成！所有检查通过。

📋 项目信息:
   项目名: Tazten
   表名: transactions
   URL: https://xxx.supabase.co
```

---

## 📋 验证清单

运行验证脚本前，请确认：

- [ ] 已安装依赖 (`npm install`)
- [ ] 已创建 `.env.local` 文件
- [ ] 已填入 Supabase URL
- [ ] 已填入 Supabase Anon Key
- [ ] 已填入 Supabase Service Role Key
- [ ] 已在 Supabase Dashboard 执行 `schema.sql`
- [ ] transactions 表已创建

---

## 🔍 如果验证失败

### 问题1: 表不存在

**错误**: `relation "transactions" does not exist`

**解决**:
1. 登录 Supabase Dashboard
2. 进入 SQL Editor
3. 执行 `supabase/schema.sql` 中的 SQL

### 问题2: 字段不匹配

**错误**: `column "xxx" does not exist`

**解决**:
1. 检查 `supabase/schema.sql` 中的字段定义
2. 在 Supabase Dashboard → Table Editor 中查看实际表结构
3. 使用 ALTER TABLE 添加缺失字段

### 问题3: 权限错误

**错误**: `permission denied` 或 `RLS policy violation`

**解决**:
1. 确认使用了 Service Role Key（不是 Anon Key）
2. 检查 RLS 策略（如果需要，可以暂时禁用 RLS 进行测试）

---

## ✅ 验证通过后

验证通过后，可以继续：

1. ✅ 测试图片上传到 TOS
2. ✅ 测试豆包 API 调用
3. ✅ 测试完整流程（上传 → 识别 → 入库）

---

**现在可以运行验证脚本了！** 🚀

