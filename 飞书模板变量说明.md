# 飞书模板变量说明

## 📋 当前代码支持的模板变量

代码会自动填充以下模板变量：

- `direction` - 支出/收入
- `direction_icon` - 📤/📥
- `amount` - 金额（如：CNY 14.73）
- `merchant` - 商户名称
- `category` - 一级分类
- `subcategory` - 二级分类
- `category_full` - 完整分类（如：出行 / 交通）
- `time` - 交易时间
- `confidence` - 置信度（如：90.0%）
- `record_id` - 记录 ID
- `created_at` - 创建时间
- `img_key` - 图片 key（默认为空字符串）
- `image_url` - 图片 URL（默认为空字符串）

## ⚠️ 错误处理

如果遇到错误：`variable xxx not found`

这说明你的模板中有一个变量 `xxx`，但代码中没有提供。解决方法：

### 方法一：在模板中移除该变量（推荐）

如果你的模板中有 `img_key` 或其他不需要的变量，可以在飞书卡片搭建工具中移除它们。

### 方法二：在代码中添加该变量

如果确实需要这个变量，可以修改 `lib/feishu/notify.ts` 文件中的 `templateVariable` 对象，添加对应的变量。

例如，如果需要 `img_key` 变量（用于显示图片），可以：

1. 先上传图片到飞书，获取 `image_key`
2. 在代码中添加：
   ```typescript
   img_key: '你的image_key' // 或从 transaction.image_url 获取
   ```

## 🔍 如何查看模板变量

1. 访问飞书卡片搭建工具：https://open.feishu.cn/tool/cardbuilder
2. 打开你的模板
3. 查看所有使用的变量占位符（如 `${img_key}`, `${amount}` 等）
4. 确保这些变量都在代码的 `templateVariable` 对象中

## 💡 建议

- 如果不需要图片，建议在模板中移除 `img_key` 变量
- 如果确实需要图片，需要先上传图片获取 `image_key`，然后添加到代码中

