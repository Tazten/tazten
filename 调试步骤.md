# 调试步骤

## 🔍 查看详细错误日志

现在代码已经添加了详细的日志输出。当你再次上传图片时，在终端中应该能看到：

```
📤 开始上传图片到 Supabase Storage...
📁 文件名: xxx.jpg
📏 文件大小: xxx bytes
...
```

## 📋 请检查终端输出

上传图片后，请把终端中的**完整日志**发给我，包括：

1. **上传步骤的日志**（如果有）
2. **豆包 API 调用的日志**（如果有）
3. **数据库保存的日志**（如果有）
4. **任何错误信息**

## 🔧 常见问题快速检查

### 1. Supabase Storage Bucket

确认 `bill-images` bucket 已创建：
- 登录 Supabase Dashboard
- 进入 Storage
- 确认 `bill-images` 存在
- 确认设置为**公开访问**（Public bucket）

### 2. 环境变量

确认 `.env.local` 中配置了：
```bash
NEXT_PUBLIC_SUPABASE_URL=...
SUPABASE_SERVICE_ROLE_KEY=...
DOUBAO_API_KEY=...
DOUBAO_MODEL_ENDPOINT=...
```

### 3. RLS 策略

如果数据库操作失败，执行：
```sql
ALTER TABLE transactions DISABLE ROW LEVEL SECURITY;
```

## 🚀 下一步

1. **重新上传图片**
2. **查看终端日志**
3. **把完整的日志发给我**

这样我就能精确定位问题了！

