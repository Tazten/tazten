# 环境变量配置指南

## 📁 文件位置

环境变量文件位于项目根目录：

```
tazten/
├── .env.local          ← 在这里配置（已创建，请填入你的实际值）
├── .env.example        ← 示例文件（参考用）
└── ...
```

## 🔑 需要配置的变量

### 1. Supabase 配置（必须）

在 Supabase Dashboard 获取：

1. 访问 https://app.supabase.com
2. 选择项目 **Tazten**
3. 进入 **Settings** → **API**
4. 复制以下值到 `.env.local`：

```bash
# Project URL
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co

# anon public key（公开的，前端可用）
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# service_role key（保密的，仅服务端使用）
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### 2. 火山引擎 TOS 配置（必须）

在火山引擎控制台获取：

1. 访问 https://console.volcengine.com/tos
2. 进入 **Access Key 管理**
3. 创建或查看 Access Key
4. 复制以下值到 `.env.local`：

```bash
TOS_ACCESS_KEY=your-access-key-id
TOS_SECRET_KEY=your-secret-access-key
TOS_ENDPOINT=tos-cn-beijing.volces.com  # 根据你的区域调整
TOS_REGION=cn-beijing                    # 根据你的区域调整
TOS_BUCKET_NAME=tazten                   # 你的存储桶名称
```

### 3. 火山引擎豆包 API 配置（必须）

在火山引擎控制台获取：

1. 访问 https://console.volcengine.com
2. 开通 **豆包视觉理解** 服务
3. 创建 API Key
4. 复制以下值到 `.env.local`：

```bash
DOUBAO_API_KEY=your-doubao-api-key
DOUBAO_BASE_URL=https://ark.cn-beijing.volces.com/api/v3  # 根据实际 API 文档调整
```

## 📝 配置步骤

### 步骤 1: 打开 `.env.local` 文件

文件位置：`/Users/tangziteng/github/tazten/.env.local`

### 步骤 2: 填入实际值

将上面获取的值替换到 `.env.local` 文件中。

### 步骤 3: 保存文件

保存后，环境变量会自动加载。

## ⚠️ 注意事项

1. **不要提交 `.env.local` 到 Git**
   - 已在 `.gitignore` 中排除
   - 包含敏感信息

2. **Vercel 部署时需要配置**
   - 在 Vercel Dashboard → Project Settings → Environment Variables
   - 添加所有环境变量

3. **本地开发**
   - `.env.local` 文件只在本地生效
   - Next.js 会自动加载

## ✅ 验证配置

配置完成后，运行验证脚本：

```bash
npm run verify:supabase
```

这会检查 Supabase 配置是否正确。

## 🔍 快速检查清单

- [ ] `.env.local` 文件已创建
- [ ] `NEXT_PUBLIC_SUPABASE_URL` 已填入
- [ ] `NEXT_PUBLIC_SUPABASE_ANON_KEY` 已填入
- [ ] `SUPABASE_SERVICE_ROLE_KEY` 已填入
- [ ] `TOS_ACCESS_KEY` 已填入
- [ ] `TOS_SECRET_KEY` 已填入
- [ ] `TOS_ENDPOINT` 已填入
- [ ] `TOS_REGION` 已填入
- [ ] `TOS_BUCKET_NAME` 已填入
- [ ] `DOUBAO_API_KEY` 已填入
- [ ] `DOUBAO_BASE_URL` 已填入

---

**配置完成后告诉我，我们继续下一步！** 🚀

