# Supabase 验证步骤

## ✅ Supabase 配置已完成

现在需要验证连接是否正常。

## 📋 验证步骤

### 1. 安装依赖（如果还没安装）

在你的终端执行：

```bash
cd /Users/tangziteng/github/tazten
npm install
```

### 2. 确认数据库表已创建

在 Supabase Dashboard 中：

1. 登录 [Supabase Dashboard](https://app.supabase.com)
2. 选择项目 **Tazten**
3. 进入 **SQL Editor**
4. 执行以下 SQL（或直接执行 `supabase/schema.sql` 文件中的内容）：

```sql
-- 创建 transactions 表
CREATE TABLE IF NOT EXISTS transactions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  direction TEXT NOT NULL CHECK (direction IN ('expense', 'income')),
  amount NUMERIC(15, 2) NOT NULL,
  currency TEXT NOT NULL DEFAULT 'CNY',
  occurred_at TIMESTAMPTZ,
  merchant TEXT,
  category TEXT,
  subcategory TEXT,
  note TEXT,
  confidence NUMERIC(3, 2) NOT NULL CHECK (confidence >= 0 AND confidence <= 1),
  raw_text TEXT,
  status TEXT NOT NULL DEFAULT 'success' CHECK (status IN ('success', 'needs_review', 'failed')),
  image_url TEXT NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 创建索引
CREATE INDEX IF NOT EXISTS idx_transactions_created_at ON transactions(created_at DESC);
CREATE INDEX IF NOT EXISTS idx_transactions_status ON transactions(status);
CREATE INDEX IF NOT EXISTS idx_transactions_direction ON transactions(direction);
```

5. 点击 **Run** 执行 SQL

### 3. 运行验证脚本

在你的终端执行：

```bash
cd /Users/tangziteng/github/tazten
npm run verify:supabase
```

或者如果 tsx 没有安装：

```bash
npx tsx scripts/verify-supabase.ts
```

### 4. 查看验证结果

如果一切正常，你会看到：

```
✅ Supabase 验证完成！所有检查通过。

📋 项目信息:
   项目名: Tazten
   表名: transactions
   URL: https://xxx.supabase.co
```

## 🔍 如果验证失败

### 问题1: 表不存在

**错误**: `relation "transactions" does not exist`

**解决**: 回到步骤 2，执行 SQL 创建表

### 问题2: 连接失败

**错误**: `Invalid API key` 或 `Failed to fetch`

**解决**: 
- 检查 `.env.local` 中的配置是否正确
- 确认 Supabase URL 和 Keys 都已正确填写
- 确认没有多余的空格或引号

### 问题3: 权限错误

**错误**: `permission denied`

**解决**: 
- 确认使用了 Service Role Key（不是 Anon Key）
- 检查 RLS 策略（如果需要，可以暂时禁用）

## ✅ 验证通过后

验证通过后，我们可以继续：

1. ✅ 配置 TOS（图片上传）
2. ✅ 配置豆包 API（图片识别）
3. ✅ 测试完整流程

---

**请先执行步骤 2（创建表），然后运行验证脚本！** 🚀

