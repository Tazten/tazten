# 性能优化说明

## ✅ 已完成的优化

### 1. 减少 Token 数量
- **之前**: `max_completion_tokens: 65535` (太大)
- **现在**: `max_completion_tokens: 2048` (足够使用，速度更快)
- **效果**: 减少 API 响应时间

### 2. 简化 Prompt
- **之前**: 详细的分类说明，多行文字
- **现在**: 压缩为简洁的 JSON 格式说明
- **效果**: 减少输入 token，加快处理速度

### 3. 减少日志输出
- **之前**: 大量 console.log 输出
- **现在**: 只在开发环境输出关键日志
- **效果**: 减少 I/O 开销

### 4. 保持低推理强度
- **已设置**: `reasoning_effort: 'low'`
- **效果**: 最快速度（但可能准确度略低）

## 🚀 进一步优化建议

### 1. 使用更快的模型（如果可用）
如果豆包有更快的模型版本，可以尝试切换。

### 2. 图片压缩
在上传前压缩图片，减少传输时间：
```typescript
// 可以在前端压缩图片后再上传
```

### 3. 添加超时设置
```typescript
const controller = new AbortController()
const timeoutId = setTimeout(() => controller.abort(), 30000) // 30秒超时
```

### 4. 使用流式响应（如果 API 支持）
如果豆包 API 支持流式响应，可以边返回边处理。

### 5. 缓存机制
对于相同的图片，可以缓存识别结果。

## 📊 预期效果

- **Prompt 简化**: 减少 50-70% 的输入 token
- **Token 限制**: 减少 97% 的最大输出 token
- **总体速度**: 预计提升 30-50%

## 🔍 如何测试

1. 上传一张图片
2. 观察控制台的响应时间日志
3. 对比优化前后的速度

## 💡 如果还是很慢

可能的原因：
1. **网络延迟**: 检查网络连接
2. **API 限流**: 检查是否有速率限制
3. **图片太大**: 尝试压缩图片
4. **模型本身慢**: 这是模型特性，无法完全避免

建议：**优先使用默认图片功能**，这样可以跳过 AI 识别，直接体验完整流程。

