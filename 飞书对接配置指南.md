# 飞书卡片消息对接配置指南

## 📋 功能说明

当有新的账单数据成功入库后，系统会自动发送一条飞书卡片消息通知你，让你及时了解新记录。

## 🔧 你需要做什么？

### 第一步：创建飞书应用并获取凭证

1. **创建飞书应用**
   - 访问：https://open.feishu.cn/
   - 使用你的飞书账号登录
   - 点击"创建企业自建应用"
   - 填写应用名称（如：无感记账通知）
   - 选择应用图标
   - 点击"创建"

2. **获取应用凭证**
   - 在应用管理页面，进入"凭证与基础信息"
   - 获取 `App ID` 和 `App Secret`

3. **配置权限**
   - 进入"权限管理"
   - 开启以下权限：
     - `im:message` - 发送单聊、群聊消息
     - `im:message:group_at_msg:readonly` - 获取群聊中@机器人消息
   - 保存并提交审核（企业自建应用通常自动通过）

4. **获取接收者 ID**
   - **群聊 ID（chat_id）**：在群聊中，点击群设置 → 群信息，可以看到群 ID
   - **用户 ID（open_id）**：需要调用 API 获取，或使用飞书开发者工具查询
   - 从你的 curl 示例看，`receive_id` 是 `ou_` 开头，这是 `open_id` 类型

### 第二步：配置环境变量

在项目的 `.env.local` 文件中添加以下配置：

```bash
# 飞书应用凭证（必需）
FEISHU_APP_ID=你的AppID
FEISHU_APP_SECRET=你的AppSecret

# 接收者信息（必需）
FEISHU_RECEIVE_ID=你的群聊ID或用户ID（如：ou_7d8a6e6df7621556ce0d21922b676706ccs）
FEISHU_RECEIVE_ID_TYPE=open_id  # 可选：chat_id、open_id、user_id，默认 open_id

# 模板卡片配置（可选，如果使用模板卡片）
FEISHU_USE_TEMPLATE=true
FEISHU_TEMPLATE_ID=你的模板ID
FEISHU_TEMPLATE_VERSION=1.0.0
```

**参考文档：**
- Open API: https://open.feishu.cn/document/server-docs/im-v1/message/create

### 第三步：测试功能

1. 启动开发服务器：`npm run dev`
2. 上传一张账单图片
3. 等待识别完成
4. 检查你的飞书，应该会收到一条卡片消息

## 📱 卡片消息内容

飞书卡片会显示以下信息：

- **标题**：新账单记录（支出/收入图标）
- **金额**：交易金额和货币
- **商户**：商户名称
- **分类**：一级分类 / 二级分类
- **时间**：交易发生时间
- **置信度**：AI 识别置信度
- **记录 ID**：数据库记录 ID
- **创建时间**：记录创建时间

## 🎨 使用模板卡片（可选，推荐）

如果你想使用飞书卡片模板，可以：

1. **访问飞书卡片搭建工具**
   - 打开：https://open.feishu.cn/tool/cardbuilder
   - 使用飞书账号登录

2. **创建卡片模板**
   - 点击"新建卡片"
   - 设计卡片样式（标题、字段、布局等）
   - 使用变量占位符，例如：
     - `${direction}` - 支出/收入
     - `${amount}` - 金额
     - `${merchant}` - 商户
     - `${category}` - 分类
     - `${time}` - 时间
     - `${confidence}` - 置信度
     - `${record_id}` - 记录 ID

3. **保存并获取模板 ID**
   - 保存模板后，会生成 `template_id`
   - 记录模板版本号（如 `1.0.0`）

4. **配置环境变量**
   ```bash
   FEISHU_TEMPLATE_ID=你的模板ID
   FEISHU_TEMPLATE_VERSION=1.0.0
   ```

5. **优势**
   - 样式统一，易于维护
   - 可以在飞书 UI 中直接修改，无需改代码
   - 支持更丰富的交互功能

参考文档：https://open.feishu.cn/document/cardkit-v1/feishu-card-resource-overview

## ⚙️ 配置说明

### 如果不想接收通知

如果不配置 `FEISHU_WEBHOOK_URL`，系统会跳过通知，不影响其他功能。

### 如果通知发送失败

通知发送失败不会影响主流程（数据仍然会正常保存），只会在控制台记录错误日志。

## 🔍 验证配置

配置完成后，可以通过以下方式验证：

1. **查看日志**：在开发环境中，控制台会显示"✅ 飞书通知发送成功"
2. **检查飞书**：上传账单后，检查飞书是否收到消息
3. **检查错误**：如果没收到，查看控制台是否有错误信息

## 💡 注意事项

- Webhook URL 是保密的，不要公开分享
- 如果更换了 Webhook URL，需要更新环境变量并重启服务
- 生产环境（Vercel）也需要配置相同的环境变量

