# 环境变量更新说明

## ✅ 已更新

根据你提供的 curl 示例，我已经更新了代码：

### 1. 支持两种环境变量名

- `ARK_API_KEY`（优先，匹配 curl 示例）
- `DOUBAO_API_KEY`（兼容旧配置）

### 2. BASE_URL 已硬编码

豆包的 API 端点是固定的：`https://ark.cn-beijing.volces.com/api/v3`

**不需要**在 `.env.local` 中配置 `DOUBAO_BASE_URL` 了。

## 📝 更新后的环境变量

在 `.env.local` 中，只需要配置：

```bash
# 方式1：使用 ARK_API_KEY（推荐，匹配 curl 示例）
ARK_API_KEY=your-ark-api-key
DOUBAO_MODEL_ENDPOINT=ep-20260115161308-w22bh

# 方式2：使用 DOUBAO_API_KEY（兼容）
DOUBAO_API_KEY=your-doubao-api-key
DOUBAO_MODEL_ENDPOINT=ep-20260115161308-w22bh
```

## 🔧 需要做的

1. **更新 `.env.local`**：
   - 如果使用 `ARK_API_KEY`，确保变量名正确
   - 如果使用 `DOUBAO_API_KEY`，保持不变也可以
   - **删除** `DOUBAO_BASE_URL`（如果存在）

2. **确认 Endpoint ID**：
   - `DOUBAO_MODEL_ENDPOINT` 或 `ARK_MODEL_ENDPOINT`
   - 值应该是你的实际 endpoint ID：`ep-20260115161308-w22bh`

3. **重启开发服务器**：
   ```bash
   # 停止当前服务器（Ctrl+C）
   npm run dev
   ```

## ✅ 总结

**必需的环境变量**：
- ✅ `ARK_API_KEY` 或 `DOUBAO_API_KEY`（二选一）
- ✅ `DOUBAO_MODEL_ENDPOINT` 或 `ARK_MODEL_ENDPOINT`（endpoint ID）

**不需要的环境变量**：
- ❌ `DOUBAO_BASE_URL`（已硬编码）

---

**更新 `.env.local` 后，重启服务器，然后重新测试！** 🚀

