# 飞书模板卡片配置说明

## 📋 环境变量配置

根据你提供的 curl 示例，你的 `receive_id` 是 `ou_` 开头，这是 `open_id` 类型。

在 `.env.local` 文件中添加以下配置：

```bash
# 飞书应用凭证（在飞书开放平台获取）
FEISHU_APP_ID=你的AppID
FEISHU_APP_SECRET=你的AppSecret

# 接收者信息（从你的 curl 示例看，这是 open_id）
FEISHU_RECEIVE_ID=ou_7d8a6e6df7621556ce0d21922b676706ccs
FEISHU_RECEIVE_ID_TYPE=open_id

# 模板卡片配置（如果使用模板）
FEISHU_USE_TEMPLATE=true
FEISHU_TEMPLATE_ID=你的模板ID
FEISHU_TEMPLATE_VERSION=1.0.0
```

## 🔍 如何获取这些值

### 1. App ID 和 App Secret
- 访问：https://open.feishu.cn/
- 登录后进入你的应用
- 在"凭证与基础信息"页面获取

### 2. Receive ID 和 Receive ID Type
- **open_id**（你的情况）：`ou_` 开头，用于单聊用户
- **chat_id**：`oc_` 开头，用于群聊
- **user_id**：`ou_` 开头，但需要特殊权限

从你的 curl 示例看，`receive_id` 是 `ou_7d8a6e6df7621556ce0d21922b676706ccs`，所以：
- `FEISHU_RECEIVE_ID=ou_7d8a6e6df7621556ce0d21922b676706ccs`
- `FEISHU_RECEIVE_ID_TYPE=open_id`

### 3. 模板 ID 和版本
- 访问飞书卡片搭建工具：https://open.feishu.cn/tool/cardbuilder
- 创建或选择你的模板
- 获取 `template_id` 和版本号

## 📝 模板变量说明

如果你使用模板卡片，需要在模板中定义以下变量（变量名可以自定义，但需要在代码中对应）：

**建议的模板变量名：**
- `direction` - 支出/收入
- `direction_icon` - 📤/📥
- `amount` - 金额（如：CNY 14.73）
- `merchant` - 商户名称
- `category` - 分类（如：出行 / 交通）
- `time` - 时间
- `confidence` - 置信度（如：90.0%）
- `record_id` - 记录 ID

**注意：** 如果模板中的变量名不同，需要修改 `lib/feishu/notify.ts` 中的 `templateVariable` 对象。

## 🧪 测试

配置完成后：
1. 重启开发服务器：`npm run dev`
2. 上传一张账单图片
3. 等待识别完成
4. 检查飞书是否收到模板卡片消息

## ⚠️ 注意事项

1. **模板变量匹配**：确保模板中的变量名与代码中的变量名一致
2. **权限配置**：确保应用有"发送消息"权限
3. **Token 有效期**：Access Token 会自动获取和刷新
4. **错误处理**：如果发送失败，不会影响主流程，只会在控制台记录错误

