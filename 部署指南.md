# 部署指南

## 第一步：Supabase 配置

### 1. 创建 Supabase 项目

1. 访问 [Supabase](https://supabase.com)
2. 创建新项目
3. 记录项目 URL 和 API Keys

### 2. 创建数据库表

在 Supabase Dashboard → SQL Editor 中执行 `supabase/schema.sql` 文件中的 SQL 语句。

### 3. 创建 Storage Bucket（可选，如果使用 Supabase Storage）

1. 进入 Storage 页面
2. 创建新 bucket，名称：`bill-images`
3. 设置为公开访问（Public bucket）

### 4. 获取 API Keys

- `NEXT_PUBLIC_SUPABASE_URL`: 项目 URL
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`: Settings → API → anon public key
- `SUPABASE_SERVICE_ROLE_KEY`: Settings → API → service_role key（保密！）

---

## 第二步：火山引擎 TOS 配置

### 1. 创建存储桶

1. 访问 [火山引擎 TOS 控制台](https://console.volcengine.com/tos)
2. 创建存储桶，名称：`tazten`（或自定义）
3. 设置为公开读取（用于直接访问图片）

### 2. 获取访问凭证

1. 进入 Access Key 管理
2. 创建 Access Key 和 Secret Key
3. 记录以下信息：
   - `TOS_ACCESS_KEY`
   - `TOS_SECRET_KEY`
   - `TOS_ENDPOINT`（如：tos-cn-beijing.volces.com）
   - `TOS_REGION`（如：cn-beijing）

---

## 第三步：火山引擎豆包 API 配置

### 1. 开通服务

1. 访问 [火山引擎控制台](https://console.volcengine.com)
2. 开通「豆包视觉理解」服务
3. 创建 API Key

### 2. 获取配置

- `DOUBAO_API_KEY`: API Key
- `DOUBAO_BASE_URL`: API 端点（如：https://ark.cn-beijing.volces.com/api/v3）

> **注意**：豆包 API 的具体调用方式可能需要根据实际文档调整，当前代码中的实现是示例。

---

## 第四步：本地开发

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

复制 `.env.example` 为 `.env.local`：

```bash
cp .env.example .env.local
```

填入所有配置项。

### 3. 运行开发服务器

```bash
npm run dev
```

访问 http://localhost:3000

---

## 第五步：Vercel 部署

### 1. 推送代码到 GitHub

```bash
git add .
git commit -m "Initial commit"
git push origin main
```

### 2. 在 Vercel 中部署

1. 访问 [Vercel](https://vercel.com)
2. 导入 GitHub 仓库
3. 配置环境变量（所有 `.env.local` 中的变量）
4. 部署

### 3. 环境变量配置

在 Vercel Dashboard → Project Settings → Environment Variables 中添加：

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `TOS_ACCESS_KEY`
- `TOS_SECRET_KEY`
- `TOS_ENDPOINT`
- `TOS_REGION`
- `TOS_BUCKET_NAME`
- `DOUBAO_API_KEY`
- `DOUBAO_BASE_URL`

---

## 测试清单

- [ ] Supabase 表创建成功
- [ ] TOS 存储桶创建成功
- [ ] 豆包 API Key 配置正确
- [ ] 本地开发服务器运行正常
- [ ] 上传图片功能正常
- [ ] 图片成功上传到 TOS
- [ ] 豆包 API 调用成功
- [ ] 数据成功保存到 Supabase
- [ ] 历史记录页面正常显示
- [ ] Vercel 部署成功

---

## 常见问题

### 1. TOS 上传失败

- 检查 Access Key 和 Secret Key 是否正确
- 确认存储桶名称和区域配置正确
- 确认存储桶权限设置为公开读取

### 2. 豆包 API 调用失败

- 检查 API Key 是否正确
- 确认 API 端点 URL 正确
- 查看实际 API 文档，调整请求格式

### 3. Supabase 插入失败

- 检查表结构是否正确创建
- 确认 Service Role Key 配置正确
- 检查字段类型是否匹配

