# 快速验证指南

## 问题：tsx 命令未找到

这是因为依赖还没有安装。按以下步骤操作：

## 解决步骤

### 1. 安装依赖

在你的终端执行：

```bash
cd /Users/tangziteng/github/tazten
npm install
```

这会安装所有依赖，包括 `tsx`。

### 2. 再次运行验证

安装完成后，执行：

```bash
npm run verify:supabase
```

## 如果 npm install 很慢

可以使用国内镜像：

```bash
npm install --registry=https://registry.npmmirror.com
```

## 验证脚本会检查什么？

1. ✅ 环境变量是否配置
2. ✅ Supabase 客户端连接
3. ✅ Supabase 服务端连接
4. ✅ transactions 表是否存在
5. ✅ 表结构是否正确
6. ✅ 插入/删除测试

## 预期结果

如果一切正常，你会看到：

```
🔍 开始验证 Supabase 连接...

1️⃣ 检查环境变量:
   ✅ NEXT_PUBLIC_SUPABASE_URL: https://xxx.supabase.co
   ✅ NEXT_PUBLIC_SUPABASE_ANON_KEY: eyJ...
   ✅ SUPABASE_SERVICE_ROLE_KEY: eyJ...

2️⃣ 测试客户端连接:
   ✅ 客户端连接成功

3️⃣ 测试服务端连接:
   ✅ 服务端连接成功

4️⃣ 验证表结构:
   ✅ 表结构验证通过
   ✅ 插入测试成功，记录 ID: xxx
   ✅ 测试数据已清理

5️⃣ 检查表字段:
   ✅ 所有必需字段都存在 (共 15 个字段)

✅ Supabase 验证完成！所有检查通过。
```

## 如果验证失败

### 错误：表不存在

**解决**：在 Supabase Dashboard → SQL Editor 中执行 `supabase/schema.sql` 中的 SQL

### 错误：连接失败

**解决**：检查 `.env.local` 中的配置是否正确

---

**先执行 `npm install`，然后再运行验证！** 🚀

